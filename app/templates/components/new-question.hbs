{{#if addNewQuestion}}
  <h1>New Question</h1>
  <div>
    <form>
      <div class="form-group" required="required">
        <label for="author">First Name</label>
        {{input value=author id="author" class="form-control"}}
      </div>
      <div class="form-group required='required'">
        <label for="authorlast">Last Name</label>
        {{input value=authorlast id="authorlast" class="form-control"}}
      </div>
      <div class="form-group required='required'">
        <label for="query">Question</label>
        {{input value=query id="query" class="form-control"}}
      </div>  <div class="form-group required='required'">
          <label for="notes">Notes</label>
          {{input value=notes id="notes" class="form-control" }}
        </div>
        <button {{action 'saveQuestion1'}}>Save</button>
      </form>
  </div>
{{else}}
  <button {{action 'questionFormShow'}}>New Question</button>
{{/if}}
